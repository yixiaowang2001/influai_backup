# InfluAI

# 开始
1. 在`backend/configs`目录下，创建`credential.py`，将API放入。
2. 在`backend/ai_module`下，调整`prompts.py`，尝试运行`post_related.py`以查看效果（目前就做了一个根据人设模版和帖子，预测转发、点赞、评论量）。
3. `backend/data/test_data.py`目前存放的是测试用的数据。文档里当前的内容是一个游戏主播模版（包含人设、粉丝量、和评论者分布）。